export default class Bunker {
    constructor(rooms, finalRoom, currentRoom, player) {
        this.rooms = rooms;
        this.finalRoom = finalRoom;
        this.currentRoom = currentRoom;
        this.player = player;
    }
}